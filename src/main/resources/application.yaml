server:
  port: 9090

spring:
  application:
    name: KTM
  config:
    import: "classpath:errors.yml"

  datasource:
    url: jdbc:mysql://localhost:3306/ktm_master?serverTimezone=UTC
    username: root
    password: secret123

  jpa:
    hibernate:
#      DDL = Data Definition Language (DDL = CREATE, ALTER, DROP tables)
      ddl-auto: update

    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    show-sql: true
#    Dialect = the way a database “speaks” SQL
    database-platform: org.hibernate.dialect.MySQLDialect

  jackson:
    time-zone: UTC

logging:
  file:
    name: logs/app.log
  logback:
    rollingpolicy:
      max-file-size: 3MB
      max-history: 3
  level:
    root: INFO
    org.springframework: WARN
    com.yourpackage: DEBUG
